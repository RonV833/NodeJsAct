<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <title>List</title>
</head>
<body style="margin: auto; padding:10%; background-color: beige;" >
    <h2>{{title}}</h2>
    <table class="table  table-striped table-hover table-responsive">
        <tr>
            <th>Food Id</th>
            <th>Food Name</th>
            <th>Price</th>
            <th>Food Category</th>
            <th>Action</th>
        {{#each food}}
            <tr>
                <td>{{this.food_id}}</td>
                <td>{{this.food_name}}</td>
                <td>{{this.price}}</td>
                <td>{{this.food_category_id}}</td>
                <td>
                    <a href="/auth/updatefood/{{this.food_id}}">Edit</a> 
                    <a href="/auth/delete_food/{{this.food_id}}" onclick="return confirm('Are you sure you want to delete this record?')">Delete</a>
                </td>
                
            </tr>
        {{/each}}
    </table>
    {{#if message}}
    <h4>{{message}}</h4>
    {{/if}}
    <a href="/">BACK HOME</a>
</body>
</html>